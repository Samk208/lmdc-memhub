{
  "name": "lnmc-member-hub",
  "version": "1.0.0",
  "private": true,
  "description": "E2E test harness for the LNMC Member Hub plugin",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "typescript": "^5.5.4"
  }
}

